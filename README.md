# Seoul Air Dashboard

서울 실시간 미세먼지 현황과 생활 지수를 보여주는 정적 웹 대시보드입니다.

배포: GitHub Pages

공공데이터포털 AirKorea API 키를 기본값으로 사용하며, 화면에는 API 키 입력 UI를 노출하지 않습니다.

현재 위치의 행정동 판정은 `vuski/admdongkor`의 서울 행정동 경계 데이터를 앱 안에 포함해 브라우저에서만 처리합니다.
